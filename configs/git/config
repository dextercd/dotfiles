[user]
	email = dexter.c.dopping@gmail.com
	name = Dexter Castor Döpping
[core]
	editor = nvim
[alias]
	co = checkout
	graph = log --oneline --graph
	pull = pull --ff-only
	get-file = "!f(){ git show \"$1\" >$2;};f"
	mpull = pull --ff
	merge = merge --no-ff
	ff = merge --ff-only
[credential]
	helper = cache
[merge]
	ff = no
	commit = no
[pull]
	ff = yes
[push]
	default = upstream
